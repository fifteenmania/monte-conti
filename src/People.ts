import { FavoredGraph } from "./FavoredGraph";

export class People {
    order: number;
    amity: number[][];
    constructor(order: number, amity: number[][]) {
        this.order = order;
        this.amity = amity;
    }

    
}